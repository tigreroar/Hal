name: Keep App Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # Se ejecuta cada 10 minutos
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping App
        run: curl https://hal-the-showsmart.streamlit.app/
